{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "NODE_ENV=production npx tsx scripts/railway-database-target-preflight.ts && npm run db:deploy && NODE_ENV=production npx tsx scripts/railway-safe-mode-preflight.ts && NODE_ENV=production npm run db:preflight",
    "startCommand": "npm start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
