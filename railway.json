{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "echo '[predeploy] validating production safe mode' && NODE_ENV=production npx tsx scripts/railway-safe-mode-preflight.ts && echo '[predeploy] applying Prisma schema' && npm run db:deploy && echo '[predeploy] verifying database schema' && NODE_ENV=production npm run db:preflight",
    "startCommand": "npm start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
