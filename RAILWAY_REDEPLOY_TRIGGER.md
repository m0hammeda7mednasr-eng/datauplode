# Railway rollout trigger

This marker intentionally creates a real push event on the legacy Railway-tracked branch after it was aligned byte-for-byte with `main`.

Purpose: deploy the Shopify-first catalog linking endpoint and Linked Products backend without introducing branch drift.

Triggered: 2026-09-03T21:25:00Z
